// Token storage is intentionally empty when using httpOnly cookies for auth.
// Keeping the file to avoid breaking imports; functions are no-ops.

export const getTokens = () => null;
export const setTokens = () => undefined;
export const clearTokens = () => undefined;

